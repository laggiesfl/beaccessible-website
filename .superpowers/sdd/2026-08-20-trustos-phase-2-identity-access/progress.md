# SDD ledger — plan: docs/superpowers/plans/2026-08-20-trustos-phase-2-identity-access.md

## Pre-flight scan

| Scope | Produces / consumes | Finding or ruling |
|---|---|---|
| Task 1 | Creates the isolated Next.js package, layout, CSS and test harness | Ruling: the plan's `render(<RootLayout>)` example can produce nested-`html` jsdom warnings, contradicting pristine-output requirements. Test the document markup with server rendering or isolate the body shell while preserving the semantic assertions. Cost if wrong: a small test rewrite. |
| Task 2 | Creates environment parsers, Supabase clients and Proxy | Ruling: fake public keys in tests must satisfy the stated 20-character schema minimum so the public-boundary assertion tests the intended property. Cost if wrong: false-negative tests. |
| Task 3 | Creates schema, seed and Supabase project files | Ruling: follow the Supabase skill and generate each migration with `supabase migration new`; use the generated timestamped filename rather than manually inventing it, while preserving migration order and descriptive suffix. Cost if wrong: filenames differ from the plan but migration history remains correct. |
| Task 4 | Adds grants, RLS, private helpers and session revocation | Internally consistent after the plan's public-security-invoker/private-security-definer ruling; must use explicit grants plus RLS. |
| Task 5 | Creates the pure authorization resolver | Internally consistent; tests and role/action map align. |
| Task 6 | Creates immutable audit service and safe messages | Internally consistent; server append must use a narrowly granted wrapper while privileged implementation stays private. |
| Task 7 | Creates account actions and accessible sign-in/recovery UI | Internally consistent; recovery response remains account-enumeration safe. |
| Task 8 | Creates invitation acceptance transaction and UI | Internally consistent after the public security-invoker wrapper ruling; acceptance derives the user from `auth.uid()`. |
| Task 9 | Creates platform administration and bootstrap | Ruling: `toHaveBeenRolledBack()` in the plan is illustrative, not a real Vitest matcher. Replace it with observable transaction-state evidence and a pgTAP rollback assertion. Cost if wrong: test implementation differs while preserving the exact atomicity requirement. |
| Task 10 | Creates organisation-scoped team administration | Internally consistent; uses Task 4's service-only session-revocation wrapper and Task 6 audit service. |
| Task 11 | Copies and protects legacy modules | Internally consistent; root legacy pages remain in place and copied hashes/regression tests prove preservation. |
| Task 12 | Adds app-session enforcement, rate limits, headers and retention | Internally consistent; extends Task 4 session checks and modifies Task 2/7/8 interfaces deliberately. |
| Task 13 | Creates RLS-filtered audit viewer | Internally consistent; consumes Task 6 audit schema and Task 4 policies. |
| Task 14 | Creates deployed-browser, accessibility and tenant tests | Internally consistent; fixture helpers must provide every example variable used by the plan snippets. |
| Task 15 | Deploys preview and records evidence | Ruling: the preview URL shell variable must be captured and used in one shell session or written directly into the verification record by the executor; do not assume it persists between commands. Cost if wrong: a failed verification command, not a production change. |
| Tasks 1 → 12 | `next.config.ts` created then hardened | Compatible: Task 12 modifies the Task 1 output. |
| Tasks 2 → 12 | Supabase Proxy and auth actions created then extended with app-session checks | Compatible: Task 12 must retain cookie refresh and account behaviours from Tasks 2 and 7. |
| Tasks 3 → 4/6/8/9/10/12 | Base tables consumed by ordered migrations | Compatible: migrations are additive and must be generated in task order. |
| Tasks 4 → 5/10/11/12/14 | Database authorization interfaces feed pure resolver, admin actions, module routes, session enforcement and tests | Compatible: denial remains fail-closed at both server and database layers. |
| Tasks 6 → 7/8/9/10/11/12/13 | Audit event service is shared by every sensitive journey | Compatible: required audit failure must roll back the corresponding sensitive mutation. |
| Tasks 7 → 8/9/12/14 | Account flow feeds invitations, bootstrap, session registration and E2E | Compatible: later tasks extend rather than replace accessible form behaviour. |
| Tasks 8 → 9/12/14 | Invitation acceptance is extended for platform bootstrap and app sessions | Compatible: platform status activates only after normal invitation acceptance. |
| Tasks 9 → 10 | Platform creates organisation/licence/admin; client admin manages only their own team | Compatible: boundaries are distinct and client admins cannot create peer client admins. |
| Tasks 11 → 14/15 | Protected modules are exercised in release browsers and preview | Compatible: the preview must use unchanged legacy content and server authorization. |
| Tasks 12 → 14/15 | Security controls are tested locally and on Vercel | Compatible: missing browser or advisor evidence blocks release. |
| Tasks 13 → 14/15 | Audit viewer is tested and documented | Compatible: organisation filtering is enforced by RLS, not presentation logic. |
| Tasks 14 → 15 | Release suite becomes preview acceptance evidence | Compatible: local skips do not count as deployed evidence. |

## Task progress

Task 1: minor (deferred): host-level npm `http-proxy` warning may affect future installs only when that proxy is required; committed app and current tests/build are unaffected.
Task 1: complete (commits a8125ad..322a4be, review clean)
Task 2: complete (commits 322a4be..14bf70a, final review clean). Supabase SSR session cookies are server-managed with explicit HttpOnly/SameSite/Secure policy, response cache headers propagate, the browser client is a frozen anonymous-only facade with no auth surface, and the proxy performs authentication refresh/redirects only.
Task 2: verification — 13/13 auth tests, 14/14 full TrustOS tests, typecheck/build passed, Phase 1 regression 31 passed with 4 unchanged Chromium-dependent skips, and secret/bundle scans passed.
Task 3: implementation committed at dfd938e2011312e316eace5f3a58ca55f662f1af. Schema/config/seed/12-assertion pgTAP artifacts are complete; 14/14 TrustOS tests, typecheck/build, TOML/static checks and 31 runnable Phase 1 regressions passed. Database RED/GREEN, reset and lint are blocked and unverified because Docker and Podman are unavailable; Task 4 must not begin until the required local database evidence is obtained or the blocker is explicitly accepted.
Task 3: Important review fixes committed at eebf879c76e759021e7af626e6112e4024609cac. The schema migration now owns idempotent `trustops`/`grantflow` rows while fictional organisations/licences remain seed-only; the tracked plan has CLI-generated placeholders and explicit `migration new` commands for all six downstream migrations. Focused contract tests recorded 0/2 RED then 2/2 GREEN; the full available suite is 14/14 TrustOS plus 33 runnable root tests, typecheck/build, with 4 unchanged Chromium skips. Supabase pgTAP remains database-unverified due the unavailable Docker/Podman harness. No Task 4 brief exists: regenerate it from the corrected plan before Task 4 begins, and do not restore `202608200002_identity_access_rls.sql`.
Task 3: runtime pgTAP correction committed at bee4ca2. A clean disposable Supabase Postgres 17 branch applied the migration and confirmed the tables, but recorded RED on assertions 2–11 because short `has_table` and `col_is_pk` calls selected pgTAP description overloads. The test now uses explicit schema-aware arities with final descriptions and still plans exactly 12 assertions. Fresh local verification: 14/14 TrustOS tests, 33 runnable root tests with 4 unchanged Chromium skips, typecheck/build, and the static 12-plan/arity contract passed. Cloud rerun on project `fmqxvxoxxmwmanftxdwl` is pending; do not claim database GREEN. Task 4 remains unstarted.
Task 3: database GREEN confirmed by the controller on disposable Supabase Postgres 17 branch `fmqxvxoxxmwmanftxdwl`: the schema migration applied successfully; all 12 corrected explicit-schema pgTAP assertions returned `ok`; `seed.sql` ran twice and final counts remained module_catalog=2, organizations=2, organization_modules=3; Supabase security advisors returned zero lints. Performance advisors returned INFO only for unindexed Task 3 foreign keys and the inherited branch Auth absolute connection strategy, with no warning/error security findings. MCP cannot run CLI `db lint`; Task 3 adds no PL/pgSQL functions, so retain CLI database lint for GitHub/Docker release checks. The controller must delete the disposable branch after review. Task 4 remains unstarted.
Task 3: complete and accepted. The independent reviewer returned ACCEPT with no Critical or Important findings. Carry the INFO unindexed-foreign-key findings into Task 4 RLS/index design, including `audit_events.organization_id` before data growth or preview release. CLI `db lint` is nonblocking for Task 3 because MCP cannot execute it, Task 3 introduces no PL/pgSQL functions, and Supabase security advisors returned zero lints; retain the CLI lint in GitHub/Docker release checks. The controller successfully deleted disposable branch `c85188b7-0401-47ed-abaa-b4ef562eefee` after verification, stopping its hourly charge. Task 4 is authorized to begin from a regenerated brief based on the corrected plan.
